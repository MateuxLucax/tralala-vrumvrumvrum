function requestApiLeftRight(est, result) {
    console.log("requesting to fake api", result);
}

function requestApiAcceleration(aceleration) {
    console.log("requesting to fake aceleration api", aceleration);
}